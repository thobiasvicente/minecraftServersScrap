const Book = require('../models/Servers')
const sharp = require('sharp')
const path = require('path')
const fs = require('fs')
const upload = require('../config/upload')

module.exports = {
  async index(req, res) {
    console.log('hmmmmm')
  },
}

